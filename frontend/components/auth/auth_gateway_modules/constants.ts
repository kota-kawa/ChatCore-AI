// 認証後リダイレクトまでの待機時間（ミリ秒）
// Delay in milliseconds before redirecting after authentication
export const REDIRECT_DELAY_MS = 1200;
// モーダルが自動で閉じるまでの表示時間（ミリ秒）
// Duration in milliseconds before the modal auto-closes
export const MODAL_AUTO_CLOSE_MS = 2200;
// モーダルの閉じるアニメーションの時間（ミリ秒）
// Duration in milliseconds for the modal close animation
export const MODAL_CLOSE_ANIMATION_MS = 500;
